name: flutter_application_1
description: A new Flutter project.

publish_to: 'none' # 如果不是要发到 pub.dev，就保留这个

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 第三方库
  http: ^0.13.6
  provider: ^6.0.5
  fl_chart: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
